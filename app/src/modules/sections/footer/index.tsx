import { AppShellFooter, Title } from "@mantine/core";

const Footer = () => {
  return (
    <AppShellFooter>
      <Title order={1}>Footer</Title>
    </AppShellFooter>
  );
};

export default Footer;
